// Exercise 03 - Undo Deletion

/* Extend the delete SnackBar with an "Undo" action that restores the deleted
task if the user taps it within a few seconds. SnackBar takes an action:
SnackBarAction(...) parameter for exactly this purpose. You will need to
remember the deleted Task object and its original index so you can re-insert
it at the same position when Undo is pressed. This exercise stretches your
understanding of how SnackBar and state interact, and it turns the delete
feature into something a production app would actually ship. */


void main() {
  // your code here
}
