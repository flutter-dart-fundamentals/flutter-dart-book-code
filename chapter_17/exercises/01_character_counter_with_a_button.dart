// Exercise 01 - Character Counter with a Button

/* Modify the app so that the bottom Container does not update in real time.
Instead, add a TextButton below the TextField. When the user taps the
button, the bottom Container should display "You have entered X characters"
where X is the length of the text in the field. You will need to store the
text as the user types (using onChanged) but only update the display text
when the button is pressed. This exercise practices separating input capture
from display updates. */


void main() {
  // your code here
}
