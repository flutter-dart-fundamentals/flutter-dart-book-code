// Exercise 03 - Tap to Show a SnackBar

/* Build a ListView.builder where tapping any item shows a SnackBar at the
bottom of the screen with the text "You tapped [item name]." Use
ScaffoldMessenger.of(context).showSnackBar() inside the onTap callback. This
is the same SnackBar technique from the Chapter 18 exercises, now applied
inside an itemBuilder function. */


void main() {
  // your code here
}
