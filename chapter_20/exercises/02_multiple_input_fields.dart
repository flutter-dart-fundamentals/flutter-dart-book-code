// Exercise 02 - Multiple Input Fields

/* Add a second TextField to the dialog, one for a name and one for a message.
You will need a second local variable (like dialogMessage) to capture the
second field's onChanged value. When the user presses Save, update the main
screen to display both values, for example "Name: [name], Message:
[message]." This exercise practices managing multiple pieces of input inside
a single dialog. */


void main() {
  // your code here
}
