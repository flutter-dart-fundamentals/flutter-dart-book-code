// Exercise 03 - Wrap a Styled Container

/* Instead of wrapping a plain Text widget, create a Container using what you
learned in Chapter 13: give it a background color, some padding, a fixed
width, and a Text child. Wrap that Container in a GestureDetector with an
onTap callback. Confirm that tapping anywhere on the Container, not just the
text inside it, triggers the callback. Experiment with different sizes to
see how the tappable area changes. */


void main() {
  // your code here
}
