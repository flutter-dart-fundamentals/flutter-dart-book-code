// Exercise 04 - Mark Tasks as Favorites

/* Add a star icon to the leading slot that the user can tap to mark a task as
a favorite. Tapping the star toggles between an outlined star
(Icons.star_border) and a filled yellow star (Icons.star). Unlike the delete
action, this one is reversible, so you can let the user tap the star
directly without a confirmation dialog. This exercise reinforces the UX
principle from this chapter: reversible actions can have small, direct
targets because the worst case is just a quick re-tap. To track which tasks
are favorites you will need a second list, perhaps a List<bool> running
parallel to _todos. */


void main() {
  // your code here
}
