// Exercise 02 - Edit a Task

/* Add a second IconButton to the trailing slot, an edit icon that opens a
dialog pre-filled with the current task text. When the user changes the text
and presses Save, replace the task at that index with the new text. You will
need a Row widget in the trailing slot to hold both the edit and delete
buttons side by side. This exercise practices reusing the dialog pattern for
a different purpose, and it also introduces the idea that trailing can hold
more than one widget at a time. */


void main() {
  // your code here
}
