// Exercise 03 - Navigate on Tap

/* Instead of printing to the console when a ListTile is tapped, display a
SnackBar at the bottom of the screen with the tapped item's title. You will
need access to the BuildContext to show a SnackBar. Use
ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text('You
tapped Alarm'))) inside the onTap callback. This exercise introduces a
common UI feedback pattern you will use in many Flutter apps. */


void main() {
  // your code here
}
