// Exercise 01 - Temperature Converter

/* Write two functions: double celsiusToFahrenheit(double celsius) and double
fahrenheitToCelsius(double fahrenheit). Use the formulas F = C × 9/5 + 32
and C = (F − 32) × 5/9. Test them by converting a few values in main() and
printing the results with one decimal place using toStringAsFixed(1). Then
write an arrow function version of each and verify they produce the same
results. */


void main() {
  // your code here
}
