// Exercise 02 - List Applier

/* Write a higher-order function void applyToEach(List<int> numbers, void
Function(int) action) that calls the given function on every element in the
list. Test it from main() by passing different anonymous functions: one that
prints each number, one that prints each number doubled, and one that prints
only the even numbers. This is essentially your own version of forEach(). */


void main() {
  // your code here
}
