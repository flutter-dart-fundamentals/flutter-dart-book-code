// Exercise 03 - Named Routes

/* Rewrite the app to use Flutter's named route system instead of inline
MaterialPageRoute calls. Define routes in MaterialApp's routes parameter as
a map: {'/second': (context) => SecondScreen(), '/third': (context) =>
ThirdScreen()}. Navigate using Navigator.pushNamed(context, '/second')
instead of Navigator.push(). Named routes make navigation cleaner in apps
with many screens and are the stepping stone toward more advanced routing
libraries. */


void main() {
  // your code here
}
