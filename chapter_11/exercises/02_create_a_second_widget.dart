// Exercise 02 - Create a Second Widget

/* Create a second StatelessWidget class called HomePage that returns just the
Scaffold (with AppBar and body). Have MyApp return a MaterialApp whose home
parameter is HomePage(). This separates app-level configuration from screen-
level layout, a pattern you'll use in every multi-screen app. */


void main() {
  // your code here
}
