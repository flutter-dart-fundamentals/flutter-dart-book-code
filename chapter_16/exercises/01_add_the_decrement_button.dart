// Exercise 01 - Add the Decrement Button

/* Write a new _decrement() method in _MyHomePageState that follows the same
pattern as _increment() but decreases _count by one. Add a guard so the
counter never goes below zero: only decrement when _count > 0. Then add a
second TextButton labeled "Decrease" to the Column and connect it to
_decrement(). Place the two buttons side by side in a Row nested inside the
Column, with the SizedBox gap beneath the Row as before. Style the decrease
button with a different background color such as Colors.redAccent to make it
visually distinct from the increment button. Confirm that the counter never
drops below zero. */


void main() {
  // your code here
}
